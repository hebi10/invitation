# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 주 사용자는 여러 이벤트 페이지를 운영하는 관리자다.
- 관리자는 여러 유형의 이벤트 페이지 중 한 건을 빠르게 찾아 편집 화면으로 이동하고, 공개 상태와 고객 연결 상태를 안전하게 관리한다.
- 고객은 자신의 계정에 연결된 이벤트만 편집하며, 하객은 공개 조건을 충족한 초대장과 관련 공개 기능을 사용한다.

## Product Purpose

결혼식, 돌잔치, 생일, 일반 행사, 개업 초대장의 생성·편집·공개·소유권 연결·운영을 하나의 서비스 흐름으로 관리한다. 모바일 청첩장이 주요 유형이지만 관리자 구조는 특정 유형에 종속되지 않는다. 관리자 화면의 우선 성공 기준은 한 건을 빠르게 찾아 상태를 확인하고 필요한 조치를 안전하게 완료하는 것이다.

## Positioning

단순한 초대장 템플릿 전시가 아니라, 이벤트별 생성 위저드부터 고객 소유권 연결, 공개 상태와 노출 기간, 이미지와 방명록 운영까지 이어지는 실제 운영형 서비스다.

## Operating Context

- 관리자는 이벤트 유형별 위저드에서 신규 초안을 생성한다.
- 기존 이벤트는 `/page-wizard/{slug}`에서 관리자 또는 연결된 소유자가 편집한다.
- `/admin`에서 이벤트 목록, 공개 상태, 노출 기간, 이미지, 방명록, 고객 계정 연결을 운영한다.
- 관리자는 다수 이벤트 중 한 건을 검색하고 상태를 확인하는 작업을 자주 수행한다.
- 모바일 관리자 화면은 조회와 간단한 상태 변경만 지원하며, 복잡한 편집은 데스크톱 또는 전용 편집 화면으로 연결한다.

## Capabilities and Constraints

- Next.js와 Firebase를 사용한다.
- 관리자 인증은 Firebase Auth 계정과 `admin-users/{uid}` 권한 문서를 함께 검증한다.
- 이벤트 도메인이 저장 기준이며 공개 상태, 노출 기간, 소유권과 운영 권한을 보존해야 한다.
- 공개 라우트, API 응답 구조, 인증·인가 흐름은 관리자 UI 개선만으로 변경하지 않는다.
- 위험 작업은 명시적 확인과 진행 상태를 제공해야 한다.
- 관리자 모바일 화면은 전체 기능 동등성을 목표로 하지 않는다.

## Evidence on Hand

- 제품 및 라우트 설명: `README.md`
- 서비스 흐름과 사용자 역할: `docs/portfolio-service-overview.md`
- 모바일 연동 정책: `docs/mobile-client-editor-policy.md`
- 서버와 저장소 경계: `docs/service-repository-boundary.md`
- 관리자 구현: `src/app/admin/AdminPageClient.tsx`, `src/app/admin/_components/*`

## Product Principles

- 관리자는 설명을 읽기보다 실제 데이터를 바로 찾고 처리할 수 있어야 한다.
- 정상·오류·권한·공개 상태를 오해하지 않도록 사실 그대로 보여준다.
- 일상 작업과 위험 작업의 시각적·상호작용적 거리를 둔다.
- 관리자, 고객, 공개 방문자의 권한과 작업 범위를 분리한다.
- 모바일에서는 조회와 최소 상태 변경에 집중하고 복잡한 작업을 억지로 축소하지 않는다.

## Accessibility & Inclusion

- 핵심 조회와 상태 변경은 키보드로 완료할 수 있어야 한다.
- 상태는 색상만으로 전달하지 않는다.
- 비동기 진행, 성공, 오류를 보조기술이 인식할 수 있어야 한다.
- 모바일의 핵심 터치 대상은 충분한 크기와 간격을 제공해야 한다.
